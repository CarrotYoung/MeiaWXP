<view class='body'>

  <view  class='header-search-box'>
    <input class="header-input" confirm-type='search' placeholder-style='color:gray' placeholder='输入编号搜索'>
    </input> 
  </view>  


  <view class='sign-status-view'>
    <view class='sign-status-inter-view'>
      <text class='allNum-text'> 全部: {{allNum}}</text>
      <text class='signNum-text' > 已签到: {{signNum}}</text> 
      <text class='unsignNum-text'> 未签到: {{unsignNum}}</text> 
      <view class='center-left-line'></view>    
      <view class='center-right-line'></view> 
      <view class='bottom-line'></view>   
    </view>
  </view>

   <!-- 列表 -->
  <scroll-view scroll-y="true" class='sign-list'>
    <block wx:for-items="{{list}}" wx:key="{{item.id}}">
        <view class='list-item' id="{{item.id}}">
            <text class="name-text">{{item.name}}</text>
            <text class="title-text">{{item.title}}</text> 
            <text class="date-text">{{item.date}}</text>  
        </view> 
    </block> 

  </scroll-view> 

 

</view>